# LoveTruyen
 Nền tảng truyện &amp; tương tác trực tuyến
 - What we got: 
    +login/register 
    +books manager -> need to update more
    +crawler (still updating) -> ta có trang crawler cho admin :D
    +homepage frontend -> can still update
    +truyện hot & truyện từ mới đến cũ (homepage)
 - What we need:
    +searchpage, bộ lọc (chọn nhiều), sắp xếp default: mới -> cũ 
    (mũi tên xuống\/date && mũi tên hướng lên/\date) (true/false) (on/off)
    +dashboard (host & admin) | chỉ hiển thị truyện bản thân người đó đăng | tổng kết view | quản lý bình luận ...
    +